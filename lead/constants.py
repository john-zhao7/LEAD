from __future__ import annotations

DEFAULT_SESSION_DIR = ".lead_session"
DEFAULT_REPORT_DIR = "reports"

CONFIDENCE_DIRECT = "direct_evidence"
CONFIDENCE_STRONG = "strong_inference"
CONFIDENCE_WEAK = "weak_hypothesis"

SUPPORTED_CONFIG = ("config.yaml", "config.yml")
SUPPORTED_LOGS = ("stdout.log", "stderr.log")
